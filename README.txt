Como ejecutar el código
Desde src/ correr make ara ejecutar el código de C++. Desde metodos_iterativos.cpp
se puede cambiar la tolerancia del metodo.

Luego todas las experimentaciones estan en exp.ipynb. No todas las experimentaciones
que estan en la notebook fueron reportadas en el informe (no fueron utilizadas para conclusiones).
En cada Code Box del notebook se puede elegir un metodo. Los metodos fueron definidos en el primer
Code Box.
Las listas con resultado que se encuentran en algunos Code Box fueron resultado que obtuvimos de 
ejecutar el código, los dejamos pero para replicarlo solo es necesario correr el código.

Parado src correr make clean borra los ejecutables creados y make clean_entrys borra los archivos .txt
que se generan para comunicar C++ con Python